# DataAPI 面试题与参考答案（结合项目实战）

> 适用岗位：C++后端 / 数据平台开发 / Linux网络编程 / ETL工程师
>
> 使用方式：先背“短答案版”，再看“可展开版”。

---

## 1. 架构与整体设计

### Q1：你这个项目的整体架构是什么？
**短答案版：**
项目是“工具链式数据平台”，分为四层：
1. **数据生成/采集层**：`crtsurfdata` 生成分钟级气象站点观测数据；
2. **传输层**：`ftpgetfiles/ftpputfiles/tcpgetfiles/tcpputfiles/fileserver` 做文件分发；
3. **数据处理层**：`xmltodb` 入库，`dminingoracle` 抽取，`migratetable` 迁移，`syncinc/syncref` 同步；
4. **服务与运维层**：`webserver` 提供HTTP接口，`procctl` 做进程调度与守护，`inetd/rinetd/rinetdin` 做网络代理。

**可展开版：**
这套架构的核心是“**解耦与可替换**”。每个环节都是独立可执行程序，出现问题时可局部重启和替换，不影响全链路。比如入库慢时只扩 `xmltodb`，不需要动传输程序。

---

### Q2：为什么拆成多个工具，而不是一个单体服务？
**短答案版：**
拆分的好处是：
- **隔离故障**：单个工具崩溃不拖垮整链路；
- **独立扩缩容**：瓶颈在哪就扩哪；
- **发布风险小**：改同步逻辑不用重发web模块；
- **运维可观测**：每个进程有单独日志与心跳。

**风险与权衡：**
- 运维复杂度上升（进程多、配置多）；
- 需要统一规范日志、参数和监控；
- 跨进程链路追踪成本更高。

---

## 2. Linux 网络编程与并发

### Q3：你为什么选 epoll？
**短答案版：**
因为连接数较大时，`epoll` 在事件通知和遍历效率上优于 `select/poll`，并且支持非阻塞I/O，适合 `webserver/rinetd/fileserver` 这类长连接和高并发场景。

**可展开版：**
`select` 有fd上限且每次全量扫描，`poll` 虽无硬上限但也要线性扫描。`epoll` 只返回活跃事件，更适合代理转发和HTTP并发处理。

---

### Q4：你在代理里如何管理连接映射？
**短答案版：**
维护双向映射关系（`srcsock -> dstsock` 与 `dstsock -> srcsock`），读到一端数据就转发到对端；任一端断开时，关闭双方fd并清理映射，避免fd泄漏。

**可展开版：**
同时记录最后活跃时间，定时器触发时清理超时连接，避免“假活跃连接”长期占用资源。

---

### Q5：`webserver` 为什么分接收线程/工作线程/发送线程？
**短答案版：**
这是典型的“IO与业务解耦”：
- 接收线程负责网络事件和完整报文组装；
- 工作线程负责解析请求、查库、生成响应；
- 发送线程负责回包与连接收尾。

**价值：**
避免慢SQL阻塞socket读写，提高整体吞吐和系统稳定性。

---

## 3. 数据库与ETL设计

### Q6：`xmltodb` 如何做入库幂等？
**短答案版：**
按配置控制“只插入”或“插入失败后更新”，并结合主键/唯一键约束保证重复处理不会产生脏数据。成功/失败文件会分流到备份或错误目录，便于重放与排错。

---

### Q7：`syncinc` 和 `syncref` 怎么选？
**短答案版：**
- **`syncinc`（增量）**：数据量大、实时性要求高、可依赖递增字段（如keyid）时优先；
- **`syncref`（刷新）**：窗口较小、目标表允许覆盖式更新、对一致性要求高时使用。

**面试加分点：**
我会先用 `syncinc` 做主链路，再定时用 `syncref` 做校准，兼顾性能与一致性。

---

### Q8：如何避免增量同步丢数/重数？
**短答案版：**
- 维护“上次同步最大key”持久化；
- 每批按 `>` 读取并按批提交；
- 异常回滚后从上次成功点重启；
- 目标端加唯一约束兜底去重。

**扩展答法：**
若出现时间回拨或乱序，可改成“时间窗口 + 主键去重”双保险。

---

## 4. 稳定性、调度与运维

### Q9：`procctl` 的核心价值是什么？
**短答案版：**
统一调度周期任务和常驻服务，子进程退出后自动拉起，降低人工运维成本。

**可展开版（进阶）：**
要防止拉起风暴，可加失败退避策略（指数退避/最大重启次数）、启动健康检查、告警阈值。

---

### Q10：如果线上出现“数据延迟突然升高”，你怎么排查？
**短答案版（顺序化）：**
1. 看链路监控：采集、传输、入库、同步、接口哪个阶段变慢；
2. 看日志与队列堆积：是否某工具积压；
3. 看系统资源：CPU/内存/磁盘IO/网络；
4. 看数据库：慢SQL、锁等待、连接池；
5. 临时止血：降频/限流/扩容；
6. 复盘：补监控和告警规则。

---

## 5. 业务理解（气象数据场景）

### Q11：为什么很多气象字段用整数存储（例如0.1℃）？
**短答案版：**
为了避免浮点误差、便于跨系统传输和对账，同时节省存储，业务上再做比例换算展示。

---

### Q12：站点信息变化会影响历史数据吗？
**短答案版：**
一般不直接覆盖历史事实。站点元数据变化应按“生效时间”管理，历史数据保留原语义，查询时按时间关联对应版本。

---

### Q13：数据质量异常放在哪层处理更好？
**短答案版：**
采用分层治理：
- 采集层做基础格式校验；
- 入库前做范围/完整性校验；
- 服务层做业务规则校验与告警。

这样可以兼顾实时性与可追溯性。

---

## 6. 深挖题标准回答模板（可直接套）

### Q14：讲一个故障处理案例（模板）
**回答模板：**
- **背景**：某天同步延迟从2分钟升到20分钟；
- **定位**：先看`syncinc`日志发现批次成功率下降；再查数据库有锁等待和慢SQL；
- **处理**：调整批量大小、补索引、把高峰期刷新任务错峰；
- **结果**：延迟恢复到3分钟以内，失败重试率下降；
- **改进**：新增慢SQL告警和链路分段耗时监控。

---

### Q15：你做过的优化如何量化？
**回答模板：**
我会给出基准前后数据，例如：
- 吞吐：每分钟处理文件数从 X 提升到 Y；
- 延迟：端到端从 A 分钟降到 B 分钟；
- 资源：CPU从 C% 降到 D%。

面试官要的是“可验证的结果”，不是“感觉快了”。

---

## 7. 你可以主动说的“项目亮点”

1. **全链路能力完整**：采集、传输、入库、同步、服务化、代理、调度一体化；
2. **工程实战性强**：大量 Linux 网络编程、进程管理、DB 同步经验；
3. **可运维**：日志、心跳、定时、失败文件分流、守护重启；
4. **可扩展**：工具化拆分，新增数据源或新同步策略成本低。

---

## 8. 1分钟自我介绍（贴合本项目）

我主要做的是一个 C++ 的数据平台项目，负责从数据采集、文件传输、Oracle 入库、跨库同步到 Web 接口服务的一整套链路建设。技术上我重点做过 Linux 下的 epoll 网络编程、多线程队列解耦、增量同步和进程守护调度。项目中我更关注稳定性和可运维性，比如心跳、日志、失败重放、分层排障。我的优势是既能写底层实现，也能从业务角度保障数据时效性和一致性。

---

## 9. 面试前速记（建议背诵）

- 一致性关键词：**幂等、增量位点、唯一约束、失败重放**
- 并发关键词：**epoll、非阻塞、生产者消费者、线程解耦**
- 运维关键词：**心跳、守护重启、超时清理、日志分级**
- 业务关键词：**分钟级观测、时效性、质量校验、可追溯**

---

祝你面试顺利！如果你愿意，我还可以再给你补一版：
1) **“面试官追问版（每题3层追问）”**；
2) **“你来回答我来打分版（模拟面试）”**。
